- Support AnnotationForge::makeOrgPackageFromNCBI(). Right now the function
  uses biomaRt to retrieve data from the Ensembl Mart. It should be able to
  retrieve the same data using Ensembl.utils instead.

